node_modules/
dist/
coverage/
generated-*.json
*.progress.json
.DS_Store
*.log
scripts/__pycache__/

# Local pre-edit snapshots of questions.json (created by audit scripts/edits)
data/questions.json.bak*

# Orphan AI-generation scratch stub (no in-repo producer) — prevent git add -A commits
data/gapfill.generated.json

# Chaos bot run artifacts — large per-run jsonl + screenshots, regenerable
chaos-reports/v4-overnight-*/
chaos-reports/*.log
chaos-reports/*.jsonl

# Auto-audit / triage snapshots — local-only working state
.audit_logs/
*.local-backup.json

# 2026-05-10: chaos-bot output + test coverage (prevent git add -A near-misses)
chaos-reports/
coverage/
data/highyield.generated.json
