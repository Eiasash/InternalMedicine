[project]
name = "pyiceberg-first-table"
version = "0.1.0"
description = "Create a first Iceberg table against the Supabase REST catalog."
requires-python = ">=3.10"
dependencies = [
    "pyiceberg>=0.7.0",
    "pyarrow>=16.0.0",
    "pandas>=2.2.0",
]

[tool.uv]
package = false
